package practice_questions;

public class ListOfSunny {

    static boolean findPerfectSquare(int n)
    {
        double square_root = Math.sqrt(n);

        return (square_root-Math.floor(square_root)==0);
    }

    static void isSunny(int c)
    {

    }
}
